interface:
  display_name: "FairMind TDD"
  short_description: "Test-driven development aligned with FairMind acceptance criteria and test plans"
  brand_color: "#6366F1"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "fairmind"
      description: "FairMind platform MCP server for test plans and acceptance criteria"
