interface:
  display_name: "Backend NextJS"
  short_description: "NextJS API routes, MongoDB, NextAuth, and server-side patterns"
  brand_color: "#000000"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Library documentation lookup for NextJS, MongoDB, Mongoose"
      transport: "streamable_http"
      url: "https://mcp.context7.com/mcp"
    - type: "mcp"
      value: "mongodb"
      description: "MongoDB database access for schema inspection and queries"
