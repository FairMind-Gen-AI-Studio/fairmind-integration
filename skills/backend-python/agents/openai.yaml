interface:
  display_name: "Backend Python"
  short_description: "FastAPI, Pydantic, async patterns, and pytest testing strategies"
  brand_color: "#3776AB"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Library documentation lookup for FastAPI, Pydantic"
      transport: "streamable_http"
      url: "https://mcp.context7.com/mcp"
